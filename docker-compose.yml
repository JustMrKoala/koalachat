services:
  koalachat:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: koalachat
    ports:
      - "${KOALA_BIND:-0.0.0.0}:${KOALA_PORT:-8999}:8999"
    volumes:
      - koala-certs:/certs
    env_file:
      - path: .env
        required: false
    environment:
      - KOALA_ENV=${KOALA_ENV:-production}
      - KOALA_HOST=0.0.0.0
      - KOALA_PORT=8999
      - KOALA_FRONTEND_DIR=/app/frontend
      - SSL_CERT=/certs/cert.pem
      - SSL_KEY=/certs/key.pem
      - KOALA_ALLOW_SELF_SIGNED=${KOALA_ALLOW_SELF_SIGNED:-1}
      - TLS_SAN_IP=${KOALA_TLS_SAN_IP:-}
      - KOALA_TLS_HOST=${KOALA_TLS_HOST:-}
      - KOALA_WIPE_ON_START=${KOALA_WIPE_ON_START:-}
      - KOALA_BEHIND_PROXY=1
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8999/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

volumes:
  koala-certs: